Run `test-xctool.sh` and `test-xcodebuild.sh` to build and run tests using
`xctool` and `xcodebuild`, respectively.

*Note*: you may have to change the SDK and simulator destination depending on
what you have installed.
